import { Fira_Sans, Inter, Lobster_Two } from "next/font/google";

// If loading a variable font, you don't need to specify the font weight
export const lobster_two = Lobster_Two({ weight: "700" });
export const fira_sans = Fira_Sans({ weight: ["400", "700"] });
export const inter = Inter({ weight: ["300", "500", "700"] });
